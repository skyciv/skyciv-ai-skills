---
name: replace-with-directory-name
description: "Use when <the specific situations this applies to>, even if the user phrases it as <a paraphrase that doesn't name the domain directly>. Covers <what it actually does: the main capabilities/verbs>. Does not cover <adjacent thing that's a near-miss, if one exists>."
---

# Replace With Title

One or two sentences: what this teaches the agent and why it exists (what would otherwise get guessed wrong).

> **Prerequisite:** state anything that must happen first — another skill, an auth/session step, an environment requirement — or delete this line if there isn't one.

## Overview

What this covers, in plain terms. Assume the agent already knows general concepts (HTTP, JSON, common formats) — only state what's specific to this domain/API/codebase.

## Instructions

1. Step one — prescriptive if the operation is fragile/order-dependent, exploratory ("check X, then decide Y") if multiple valid approaches exist.
2. Step two.
3. Step three.

## Minimal working example

The smallest input/output pair that demonstrates the core use case.

```json
{
  "example": "replace with a real minimal payload"
}
```

## Gotchas

- Non-obvious, environment-specific facts that would otherwise cause a mistake. Delete this section if there genuinely are none yet — but revisit it after the first real run.

## References

- `references/<topic>.md` — load when <specific trigger condition>, not just "for more detail."
